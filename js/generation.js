class Generation {
    constructor(map) {
        this.map = map;
        console.log(this.map);
    }

    generateEnemy(chance) {

    }

    //generate ammo, weapons, aid in map
    generateItems(chance) {
        
    }
}